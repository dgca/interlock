---
'@type_of/interlock': patch
---

Add permanent workflow deletion from active and archived library cards and the workflow editor, with a Delete/Cancel confirmation. Delete versions and run history together. Block deletion while runs are active or other workflows reference the workflow.
