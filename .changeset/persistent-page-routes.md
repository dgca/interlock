---
'@type_of/interlock': patch
---

Add bookmarkable workflow and run URLs that restore the selected page on reload, preserve the selected Runs tab, and support browser Back and Forward with unsaved-edit protection.

Rename Activity to Runs throughout the UI to match the run URLs and domain terminology.
