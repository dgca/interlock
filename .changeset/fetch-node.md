---
'@type_of/interlock': minor
---

Add Fetch nodes for HTTP requests with URL interpolation, explicit fixed/input bindings, JSON bodies, and a sample-input request preview. Persist resolved requests and response outputs for inspection. Support timeouts, cancellation, explicit retries, HTTP status branching, and Fetch steps inside List groups.
