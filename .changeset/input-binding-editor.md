---
'@type_of/interlock': patch
---

Group input source selection and expected format in one Input section. Default to Previous step output, with Choose fields for named input fields, scoped node selection, and Advanced JSON. Canvas source labels select and focus referenced nodes.
