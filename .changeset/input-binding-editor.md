---
'@type_of/interlock': patch
---

Add an input-binding field editor with Previous step output as the default, scoped node selection, and Advanced JSON. Canvas source labels select and focus referenced nodes.
