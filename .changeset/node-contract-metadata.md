---
'@type_of/interlock': patch
---

Show input and output type summaries as the last metadata line on every node, including Batch groups and workflow boundaries. Keep node details above the summary and label Fetch methods as “Method: GET”.
