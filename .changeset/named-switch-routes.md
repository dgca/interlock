---
'@type_of/interlock': patch
---

Add Switch nodes with ordered JSON equality cases, named output ports, and a required default route. Configure cases with typed match values and named branches in the visual editor or through CLI and MCP workflow definitions, use input bindings and Batch item paths, and inspect the selected route in run history.

Tidy accounts for output handle positions to reduce avoidable crossings between branches, including Switch, Condition, and timed Agent routes.

Add brief node descriptions to creation and settings dialogs.
