---
'@type_of/interlock': minor
---

Serve MCP over HTTP from the running Interlock server. Connect agents using a stable local URL that survives upgrades with global or npx startup. Keep stdio available as a fallback.
