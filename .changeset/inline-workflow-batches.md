---
'@type_of/interlock': minor
---

Add Workflow Batch nodes with inline workflows, ordered item results, bounded concurrency, cancellation, and retries that preserve successful items. Edit each inline workflow in a focused canvas using the existing node and contract forms. Persist item runs against immutable published definitions for inspection and root-run work discovery. Existing referenced-workflow batches retain their settings and execution behavior.
